name: build-checks-ubuntu

on: [pull_request]

jobs:
  build-debian:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout repo
        uses: actions/checkout@v2
      - name: setup STE
        run: bash install_debian.sh

# todo: arch builder
# - https://docs.github.com/en/actions/creating-actions/creating-a-docker-container-action