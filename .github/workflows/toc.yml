on:
  pull_request:
    types: [ opened ]
name: TOC Generator
jobs:
  generateTOC:
    name: TOC Generator
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
        with:
          TARGET_BRANCH_PREFIX: dev